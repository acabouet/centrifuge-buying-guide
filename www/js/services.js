angular.module('centrifugeApp.services', [])

/**
 * A simple example service that returns some data.
 */
	// Set the base of operations for the JSON calls.
	//.factory('LabnetAPIGet', function($http) {
	//	var proto = $http.get,
	//	// Base path of the API we set up
	//		basePath = 'http://labnetinternational.com/api/anonymous/',
	//		callback = '?callback=JSON_CALLBACK';
	//
	//	return {
	//		loadEndpoint: function(path) {
	//			return proto(basePath+path+callback);
	//		}
	//	};
	//});
